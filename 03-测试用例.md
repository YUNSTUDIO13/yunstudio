# 个人工作台 · 测试用例（v2.2 云端版）

> 与《01-产品架构设计 v2.2》《02-技术架构方案 v2.2》配套。本文专注**测试策略、测试矩阵、核心用例、非功能测试**，为 M1–M6 各阶段提供验收依据。
> 最后更新：2026-08-07 | 覆盖 PRD v2.1 + **v2.2 约束修订**（Supabase 云端、账号体系、多 PC 互通）
>
> **v2.2 测试范式变更**：架构已无自研后端，前端经 `supabase-js` 直连托管 PostgreSQL；鉴权/隔离/实时同步由 Supabase Auth + RLS + Realtime 提供。因此：
> - 原「L3 API（Supertest + 自研 REST）」改为「**集成 / RLS 测试**（supabase-js 客户端 + PG 测试库断言）」；
> - 测试库从 `:memory: SQLite` 改为 **Supabase 测试项目（Free）/ 本地 PG via Docker**；
> - 加密从「本地 AES-GCM」改为「PG 端 `pgcrypto` / Vault，密钥不落前端」；
> - 备份从「本地 `.bak` 文件」改为「Supabase 托管备份 + 导出 JSON」。

---

## 0. 测试策略总览

```
┌─────────────────────────────────────────────────────────┐
│  L4  E2E（Playwright）       端到端关键旅程（6 条，含注册登录） │
│  L3  集成/RLS（Vitest+pg）    supabase-js 调用 / RLS 隔离 / 状态机 │
│  L2  组件（Testing Library） 交互组件 / 视觉令牌 / 表单 / 空错页    │
│  L1  单元（Vitest）          算法（Score）/ 文案生成 / 导出        │
└─────────────────────────────────────────────────────────┘
```

| 测试层 | 框架 / 载体 | 范围 | 占比 |
| --- | --- | --- | --- |
| 单元 Unit | Vitest | 紧迫度算法、文案生成、CSV/JSON 导出、状态机转移合法性 | 25% |
| 集成/RLS Integration | Vitest + `supabase-js` + 测试 PG | 全部表 CRUD（经客户端）、Zod 校验、RLS 行级隔离、错误码、AUDIT 留痕 | 35% |
| 组件 Component | Testing Library + Vitest | Toast / Modal / DataTable / KPICard / 状态机 Stepper / 拖拽 / 登录表单 | 20% |
| E2E | Playwright | 6 条核心旅程（注册登录 / 开机→开工 / 状态变更 / Sprint 预警 / Bug 催办 / 备份导出） | 20% |

### 0.1 测试环境

| 项 | 值 |
| --- | --- |
| 数据库 Test DB | Supabase **测试项目**（Free 层，独立 URL/Key）或 本地 PG via Docker（`postgres:16` + `pgcrypto`） |
| Auth 测试 | Supabase Auth 测试账号（2 个用户 A/B 用于 RLS 隔离断言）；邮箱确认可关闭以加速 |
| 前端 Test | jsdom + happydom |
| 并发 | 单进程分文件；集成测试对每个表做 setup/teardown（truncate + 重置 RLS 上下文） |
| 覆盖率目标 | 行 ≥80%，分支 ≥75%（services / supabase 封装优先） |
| CI | GitHub Actions（接入连接器后启用；当前本地 `npm test`） |

> ⚠️ RLS 测试必须在**真实 PG + 启用 RLS** 下执行，不能用关闭 RLS 的内存库替代——否则隔离断言失真。

---

## 1. 功能测试矩阵（Traceability）

| PRD 条目 | 功能点 | 测试类型 | 用例编号 | 优先级 |
| --- | --- | --- | --- | --- |
| 0. 账号体系 | 邮箱注册 / 登录 / 登出 / 会话持久化 / RLS 隔离 / 多 PC 同步 | Integration+E2E | TC-AUTH-01~06 | P0 |
| 1.1 待办手动+链接+排序 | 待办 CRUD / Score 排序 / 完成移除 | Integration+Unit+E2E | TC-TD-01~06 | P0 |
| 1.2 KPI Ticker | KPI 卡 CRUD / 跳转 / 过期角标 | Integration+Component | TC-KPI-01~04 | P1 |
| 2.1 需求池 | 需求 CRUD / 量化校验 / 分享文案 | Integration+E2E | TC-REQ-01~06 | P0 |
| 2.1 状态机 | 8 态流转合法性 | Unit+Integration | TC-SM-01~05 | P0 |
| 2.2 Roadmap | 拖拽排期 / 撤销 / 实时扣减 | Component+E2E | TC-RM-01~04 | P1 |
| 3.1 Sprint 看板 | 燃尽图 / 延期预警 / 4 列 | Integration+Component | TC-SP-01~04 | P1 |
| 3.2 Bug Tracker | Bug CRUD / 严重度 / 催办文案 | Integration+E2E | TC-BUG-01~05 | P1 |
| 3.2 催办 | 文案生成 / 不调外部 | Unit+Integration | TC-URGE-01~03 | P2 |
| 通用 3.2 列表规范 | 排序/分页/批量/导出 | Component+Integration | TC-GEN-01~05 | P1 |
| 通用 3.3 表单 | 必填/失焦/脏检查 | Component | TC-FORM-01~04 | P1 |
| 通用 3.6 空错页 | 6 态 | Component | TC-EMPTY-01~06 | P2 |
| NFR 加密 | 敏感字段 PG 端加密 | Integration | TC-SEC-01~03 | P1 |
| NFR 备份 | 云备份 / 导出 | Integration+Unit | TC-BAK-01~03 | P1 |
| NFR 审计 | 状态/删除留痕 | Integration | TC-AUD-01~02 | P1 |

---

## 2. 核心测试用例（详细）

### TC-AUTH-01 邮箱注册 + 自动建 profile ✅ P0

- **前置**：测试库 `profiles` 为空；Supabase Auth 邮箱确认已关闭（注册即登录）
- **步骤**：调用 `supabase.auth.signUp({ email, password })`（密码 ≥6 位）
- **期望**：
  - 返回 `data.user` 非空、`data.session` 非空（注册即登录）
  - `public.profiles` 自动出现一条 `id = auth.uid()` 的记录（触发器生效）
  - 重复同邮箱注册返回 `error`（邮箱唯一约束）

### TC-AUTH-02 登录 / 登出 ✅ P0

- **步骤**：
  1. `supabase.auth.signInWithPassword({ email, password })` → 期望 `session` 有效
  2. `supabase.auth.signOut({ scope: 'local' })` → 期望本地会话清空、`getSession()` 返回 null

### TC-AUTH-03 会话持久化（刷新不丢）✅ P0

- **步骤**：登录后刷新页面（或重开标签）→ `onAuthStateChange` 恢复会话
- **期望**：无需重新登录；`getSession()` 返回有效 session（persistSession + autoRefreshToken 生效）

### TC-AUTH-04 RLS 行级隔离（跨用户不可见）✅ P0

- **前置**：用户 A 创建 3 条 todo；用户 B 登录（独立 session）
- **步骤**：B 调用 `supabase.from('todo').select('*')`
- **期望**：B 返回 **0 行**（A 的数据被 RLS `owner = auth.uid()` 屏蔽）；用 service_role key（仅测试）可看到全部，验证策略生效而非表空

### TC-AUTH-05 未登录拦截 ✅ P0

- **步骤**：清空 session 后调用 `supabase.from('todo').select('*')`
- **期望**：返回 `error`（code `PGRST301` / 无有效 JWT），不泄露任何数据

### TC-AUTH-06 多 PC 实时同步 ✅ P1

- **前置**：用户 A 在两台「设备」（两个浏览器上下文）登录同一账号
- **步骤**：设备 1 `insert` 一条 todo → 设备 2 订阅 `postgres_changes` 频道
- **期望**：设备 2 在秒级内收到 INSERT 事件并刷新列表（Realtime 生效）

---

### TC-TD-01 新增待办 + Score 排序 ✅ P0

- **前置**：当前用户已登录；清空 `todo` 表；当前时间 `2026-08-07 09:00 +08:00`
- **步骤**：
  1. `supabase.from('todo').insert({ title:"评审 v2.3", priority:"P0", deadline_at:"2026-08-07T14:00" })`
  2. `supabase.from('todo').insert({ title:"写周报", priority:"P3", deadline_at:"2026-08-09T18:00" })`
  3. `supabase.from('todo').select('*').eq('owner', uid).order('score', {ascending:false})`
- **期望**：
  - 两条均插入成功（带 `owner = auth.uid()`）
  - 列表按 `score` 降序：`评审 v2.3` 在 `写周报` 前
  - 计算：P0 项 Score = (4-0)*30 + max(0,72-5)=120+67=187；P3 项 = (4-3)*30 + max(0,72-57)=30+15=45 ✅

### TC-TD-02 必填校验 ❌ 422

- **步骤**：`insert({ title:"" })`（经前端 Zod 拦截，或后端 PG `CHECK`/`NOT NULL`）
- **期望**：插入被拒（前端拦截不发起请求；或 PG 返回约束错误），无脏数据写入

### TC-TD-03 标题长度拦截

- **步骤**：`insert({ title:"x".repeat(51) })`
- **期望**：前端 Zod 拦截（≤50）；若绕过则 PG `CHECK` 拒绝

### TC-TD-04 完成态 24h 移除逻辑

- **步骤**：
  1. `update({ done:true })` → `done_at` 写入
  2. 模拟时间 +23h：`select` 该条仍在（done=true 加删除线）
  3. 模拟时间 +25h：`select` 今日视图按 `done_at` 过滤，该条不可见
- **期望**：23h 可见、25h 不可见（视图层过滤，云同步生效）

### TC-TD-05 逾期待办展示（Score 不减）

- **步骤**：`insert({ priority:"P1", deadline_at:"2026-08-01T00:00" })`（已过期）
- **期望**：Score = (4-1)*30 + max(0,72-(-...))=90+0=90；列表展示但标红「逾期」

### TC-TD-06 批量勾选

- **步骤**：`supabase.from('todo').update({ done:true }).in('id', [1,2,3])`
- **期望**：3 条 `done=true`、`done_at` 写入；返回成功数 3

---

### TC-REQ-01 需求新增 + 量化校验 ✅ P0

- **步骤**：
  1. `insert({ title:"AI 信息整合", value_desc:"提升效率" })` → 期望被拒（无量化，前端 Zod 或 PG 触发器拦截）
  2. `insert({ title:"AI 信息整合", value_desc:"提升效率 35%" })` → 期望成功
- **期望**：量化正则命中（`35%`）才放行；敏感字段 `value_desc` 在 PG 端加密存储（见 TC-SEC）

### TC-REQ-02 标题去重

- **步骤**：连续 `insert` 两条相同 `title:"AI 信息整合"`（trim 后）
- **期望**：第二条被拒（唯一约束 / 应用层校验），返回冲突错误

### TC-REQ-03 状态变更 + 文案生成 ✅ P0

- **步骤**：`update({ status:"已排期", target_version:3 })` + 前端生成 `shareText`
- **期望**：
  - 成功；`status` 更新为「已排期」；`target_version=3`
  - 返回 `shareText` 含「待评审 → 已排期」「目标版本 vX.Y」「PRD：url」「详情：link」
  - `requirement_log` 新增一条 `action=state-change`（写 PG，受 RLS 保护）

### TC-REQ-04 非法状态转移拦截

- **步骤**：当前状态「草稿」尝试 `update({ status:"已上线" })`
- **期望**：被拒（状态机校验在应用层或 PG 触发器），数据库不变

### TC-REQ-05 业务变动挂起 / 恢复

- **步骤**：「待评审」→ 挂起 → 恢复 → 待评审
- **期望**：两步转移均合法；日志 2 条

### TC-REQ-06 作废终态不可恢复

- **步骤**：「研发中」→「作废」；再尝试「作废」→「已上线」
- **期望**：第二次被拒（终态不可出）

---

### TC-SM-01 状态机全路径（Unit 覆盖）

- **步骤**：对状态图每一条边做 `canTransition(from,to)` 断言
- **期望**：合法边 20 条全过；随机非法边 20 条全拒

合法转移（20 条）：
```
草稿→待评审
待评审→{已排期,已挂起,作废}
已排期→{已挂起,研发中,作废}
研发中→{测试中,已挂起,作废}
测试中→{已上线,研发中,作废}
已挂起→{待评审,作废}
已上线（终）  作废（终）
```

### TC-SM-02 目标版本约束

- **步骤**：转移 `待评审→已排期` 但不带 `target_version`
- **期望**：被拒，"排期必须指定目标版本"

---

### TC-RM-01 拖拽排期 + 实时扣减 ✅ P1

- **前置**：需求 R1 在「未排期」池；版本 V1 列
- **步骤**（组件）：把 R1 卡片拖到 Kanban 的 V1 列 → `update({ target_version: V1 })`
- **期望**：
  - `requirement.target_version` 更新为 V1
  - 未排期池列表实时减少 1 条
  - Toast「已排至 V1」+「撤销」链接（5–10s 内可回退，`update({ target_version: null })`）
  - 撤销后未排期池恢复

### TC-RM-02 甘特图渲染

- **步骤**：`select` versions + requirements 渲染 Gantt
- **期望**：每条需求按 `release_date` 占位；无 `release_date` 显示在「未定」泳道

### TC-RM-03 撤销超时

- **步骤**：拖拽后等待 11s 再点撤销
- **期望**：撤销链接消失，状态已固化（不可回退）

### TC-RM-04 并发拖拽同一卡片

- **步骤**：两个请求同时把 R1 拖到 V1 和 V2
- **期望**：最终一致（以最终写为准），并写入操作日志

---

### TC-SP-01 燃尽图理想线 vs 实际线 ✅ P1

- **步骤**：`select` sprint + tasks 计算 burndown
- **期望**：
  - `ideal` 从 `total_points` 线性递减到 0
  - `actual` 长度 = 已过去天数；未来天为 null
  - 返回 `stats.elapsedPct`、`completePct`

### TC-SP-02 延期预警触发 ✅ P1

- **前置**：Sprint 时间进度 80%（已过去 8/10 天），完成故事点 40%
- **步骤**：进入 `/sprints` 页面
- **期望**：顶部黄底警告条 `role="alert"` 可见，文案"迭代进度风险：时间已过 80% 但完成仅 40%"

### TC-SP-03 警告不误报

- **前置**：时间进度 80%，完成 85%
- **步骤**：进入 `/sprints`
- **期望**：无警告条

### TC-SP-04 Task 拖拽改状态

- **步骤**：把 Task 从「待办」列拖到「完成」列 → `update({ status:'完成' })`
- **期望**：`task.status='完成'`；燃尽图 actual 该日点数下降

---

### TC-BUG-01 Bug 新增 + 严重度

- **步骤**：`insert({ title:"登录失败", severity:"Blocker", assignee:"张三", contact:"微信 xxx" })`
- **期望**：成功；列表 Card 左上红色 Blocker 徽章

### TC-BUG-02 Blocker 超时红标 ✅ P1

- **前置**：Blocker Bug 创建于 2h 前，status=待修
- **步骤**：列表加载
- **期望**：卡片显示红色「超时未修 >1h」徽章

### TC-BUG-03 一键催办文案 ✅ P2

- **步骤**：前端生成 urge-text（取自 bug 记录，不调外部）
- **期望**：
  - 文案含 `[Blocker] 登录失败 已创建 2h 仍未修复`
  - 含 `@张三（微信 xxx）`
  - **确认全程无任何外部 http 调用**（断言无出站请求）

### TC-BUG-04 催办确认弹窗

- **步骤**：点催办 → 弹窗预览 → 确认
- **期望**：剪贴板写入文案 + 站内消息中心 1 条 + Toast"已复制催办文案"

### TC-BUG-05 修复后计时停止

- **步骤**：`update({ status:"已修", fixed_at: now })`
- **期望**：红标消失；`fixed_at` 写入

---

### TC-KPI-01 KPI 卡 CRUD

- **步骤**：`insert({ label:"DAU", value:"1200", unit:"人", external_url:"https://shence.xxx" })`
- **期望**：成功；Dashboard 卡片展示

### TC-KPI-02 跳转新窗

- **步骤**：点击 KPI 卡
- **期望**：`window.open(external_url, '_blank', 'noopener,noreferrer')`；不污染当前页

### TC-KPI-03 过期角标

- **前置**：KPI 卡 `updated_at` 距今 >7 天
- **步骤**：Dashboard 加载
- **期望**：卡片右上「数据过期」小角标

### TC-KPI-04 手动值不告警

- **步骤**：连续两次值持平
- **期望**：系统无任何标红/告警（PRD：不接自动环比）

---

### TC-GEN-01 表格排序

- **步骤**：点表头「优先级」切换 ASC/DESC
- **期望**：列表顺序正确翻转；URL `?sort=priority&order=desc` 同步

### TC-GEN-02 查询条件保留 + 分享

- **步骤**：筛选 `status=已排期` 后复制 URL
- **期望**：打开 URL 自动带筛选条件

### TC-GEN-03 分页

- **步骤**：列表 50 条；切 15/30/50；跳页
- **期望**：每页条数正确；显示"共 50 条"

### TC-GEN-04 批量删除确认

- **步骤**：选中 3 条 → 批量删除 → 确认框显示"将删除 3 条"
- **期望**：确认后 3 条移除（仅本用户数据，受 RLS 保护）；部分失败区分"全部/部分失败"

### TC-GEN-05 导出 JSON/CSV

- **步骤**：前端导出当前用户数据 `export({ type:'csv', entity:'requirement' })`
- **期望**：返回文件；字段与列对齐；敏感字段不导出明文（仍密文或脱敏）

---

### TC-FORM-01 必填失焦红框

- **步骤**：进需求新增表单，留空标题，点别处
- **期望**：标题输入框红框 + 左对齐红字"请输入标题"

### TC-FORM-02 提交时未填拦截

- **步骤**：直接点提交
- **期望**：所有必填项红框；阻止提交

### TC-FORM-03 脏检查

- **步骤**：编辑表单改一字 → 点返回
- **期望**：弹"您有未保存的内容，确定离开吗？"

### TC-FORM-04 URL 格式校验

- **步骤**：source_url 填 `not-a-url`
- **期望**：红字"链接格式不合法（需 http/https 开头）"

---

### TC-EMPTY-01~06 六态空错页

| 用例 | 触发 | 期望 |
| --- | --- | --- |
| 01 暂无数据 | 待办列表空 | 空盒子 + "去添加待办" |
| 02 未查到结果 | 搜索无匹配 | 放大镜 + "修改查询条件" |
| 03 未登录 | 未登录访问受保护页 | 锁 + "去登录"（跳转 `/login`） |
| 04 404 | 访问 `/nope` | 迷失 + "返回首页" |
| 05 错误 | supabase 调用抛错 | 齿轮 + "重新加载" |
| 06 网络断开 | 断网刷新 | WiFi 断 + "重新连接" |

---

## 3. 非功能测试用例（NFR）

### TC-NFR-01 首屏 ≤1.5s

- **步骤**：本地 / 静态托管启动；Lighthouse / Performance API 测 `load`
- **期望**：FCP ≤0.8s，LCP ≤1.5s（云端数据经 CDN，弱网降级骨架屏）

### TC-NFR-02 大数据虚拟滚动

- **步骤**：seed 1200 条 todo；渲染列表
- **期望**：DOM 节点 ≤可视区 2x；滚动流畅无白屏

### TC-SEC-01 敏感字段 PG 端加密 ✅ P1

- **步骤**：
  1. `insert({ value_desc:"提升 35%" })`（经 supabase-js）
  2. 用 `psql` / Supabase SQL Editor 直查底层表 `SELECT value_desc FROM requirement WHERE id=1`
- **期望**：底层存储为密文（pgcrypto / Vault 加密列）；应用经 RLS + 解密视图/函数读到明文

### TC-SEC-02 跨用户不可读密文 ✅ P1

- **步骤**：用户 B 用自身 session 查用户 A 的 requirement
- **期望**：返回 0 行（RLS 先于解密生效，B 拿不到 A 的密文）

### TC-SEC-03 密钥不落前端

- **步骤**：审查前端打包产物 / 网络请求
- **期望**：无加密密钥出现在 `window` / 源码 / 请求体；加解密在 PG 端完成

### TC-BAK-01 云备份 / 导出 ✅ P1

- **步骤**：Supabase 项目开启自动备份；或前端「导出 JSON」下载当前用户全量数据
- **期望**：自动备份按保留策略留存；导出文件含 requirements/todos/bugs（敏感字段脱敏）

### TC-BAK-02 导出不影响他人

- **步骤**：用户 A 导出
- **期望**：仅含 A 的数据（RLS 限定 owner），不含 B 的任何记录

### TC-AUD-01 状态/删除留痕 ✅ P1

- **步骤**：创建 + 改状态 + 删除 一条需求
- **期望**：`requirement_log` 至少 3 条（create/state-change/delete），含 before/after（密文或脱敏），且受 RLS 保护

### TC-NFR-07 注入防护

- **步骤**：`insert({ title:"'; DROP TABLE todo; --" })`
- **期望**：参数化 + RLS 上下文，DB 原样保存字符串；无注入（PG 驱动参数化天然防护）

### TC-NFR-08 富文本 XSS 防护

- **步骤**：`value_desc` 含 `<script>alert(1)</script>`
- **期望**：存储后经 DOMPurify 渲染，脚本不执行

### TC-NFR-09 a11y AA

- **步骤**：axe-core 扫描
- **期望**：0 个 critical/serious 问题；交互元素 aria-label + 焦点可见

### TC-NFR-10 键盘可达

- **步骤**：Tab 遍历 + Enter 提交 + Esc 关弹窗 + Ctrl+S 保存
- **期望**：全部可达可用

---

## 4. E2E 旅程用例（Playwright）

### E2E-00 注册 + 登录（P0）✅ 新增

```
打开 /login → 切「注册」→ 填邮箱+密码 → 提交
期望：自动登录进入 /（profile 已建）
退出登录 → 再「登录」同账号 → 期望进入 /
```

### E2E-01 开机即开工（P0）

```
打开 /
期望：3 张 KPI 卡 + 今日待办列表 + 燃尽迷你图
点击第一条待办勾选
期望：删除线 + Toast「已标记完成」
```

### E2E-02 需求流转 + 文案（P0）

```
进入 /requirements
点「新增」填标题+量化值+PRD链接
点「提交评审」→「评审通过」并选版本
期望：状态=已排期；弹「复制通知」点击
期望：剪贴板含「待评审 → 已排期」+ 版本 + PRD 链接
```

### E2E-03 Sprint 预警（P1）

```
seed：Sprint 进度 80% / 完成 40%
进入 /sprints
期望：黄底警告条 role=alert 可见
```

### E2E-04 Bug 催办（P1）

```
进入 /bugs
新建 Blocker，assignee=张三
等待 1h（mock 时间）→ 卡片红标
点「催办」→ 确认
期望：剪贴板含催办文案 + 站内消息 1 条
```

### E2E-05 备份导出（P1）

```
进入 /data
点「导出 JSON」→ 期望下载文件含 requirements/todos/bugs（仅当前用户）
```

---

## 5. 测试数据种子（seed.ts）

| 表 | 数量 | 说明 |
| --- | --- | --- |
| auth.users + profiles | 2 | 用户 A / B（用于 RLS 隔离测试） |
| todo | 12 | 含 P0/P1/P2/P3，含 1 条已过期、1 条已完成（owner=A） |
| requirement | 8 | 覆盖所有状态各 1+ |
| version | 3 | v2.2/v2.3/v2.4 |
| sprint | 2 | 一个正常、一个触发预警 |
| task | 20 | 分配至 sprint |
| bug | 6 | 含 Blocker 超时 1 个 |
| kpi_card | 3 | 1 张过期 |
| quicklink | 5 | Jira/飞书/数仓/企微/邮箱 |
| requirement_log | 10 | 历史留痕（owner=A） |

> 种子数据须以 `owner = 用户A.uid` 写入；用户 B 的查询断言为 0 行以验证 RLS。

---

## 6. 验收门槛（Exit Criteria）

| 维度 | 门槛 |
| --- | --- |
| 单元 | 全部通过；行覆盖 ≥80% |
| 集成/RLS | TC-TD/REQ/SM/BUG/AUTH 全部通过；**含 TC-AUTH-04 跨用户隔离 0 行断言** |
| 组件 | Toast/Modal/DataTable/状态机 Stepper/登录表单 关键交互通过 |
| E2E | 6 条旅程（含注册登录）全部绿 |
| NFR | TC-SEC-01/02/03、TC-BAK-01/02、TC-AUD-01、TC-NFR-07/08 通过 |
| 安全 | 无注入 / XSS / 明文敏感字段 / 跨用户越权读 |
| 文档 | 三份设计文档（v2.2）与 PRD Checklist 全部 ✅ |

---

## 7. 缺陷严重度定义

| 级 | 定义 | 示例 |
| --- | --- | --- |
| S1 致命 | 数据丢失 / 无法启动 / 安全漏洞 | RLS 失效致跨用户读、密钥泄露前端 |
| S2 严重 | 核心链路阻断 | 状态机非法转移放行、催办误发外部、注册即登录失败 |
| S3 一般 | 功能可用但体验差 | 排序 URL 不同步、Toast 堆叠溢出 |
| S4 轻微 | 视觉/文案瑕疵 | 角标位置偏移、文案错别字 |

---

> 配套文档：**[01-产品架构设计.md](./01-产品架构设计.md)** · **[02-技术架构方案.md](./02-技术架构方案.md)**
